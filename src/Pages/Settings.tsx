import { Text, StyleSheet, View, FlatList, TouchableOpacity, TouchableWithoutFeedback, Keyboard } from 'react-native';


export default function Settings() {
    return(
        <View>
            <Text>bill clinton's bastard son</Text>
        </View>
    );
}